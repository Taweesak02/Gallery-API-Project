components:
  schemas:
    DeleteByIdNotFoundResponse:
      allOf:
        - $ref: '#/components/schemas/BaseResponse'
        - example:
            status: 404
            path: v1/auth/delete/{userId}
            message: User not found

    UpdateByIdNotFoundResponse:
      allOf:
        - $ref: '#/components/schemas/BaseResponse'
        - example:
            status: 404
            path: v1/auth/update/{userId}
            message: User not found