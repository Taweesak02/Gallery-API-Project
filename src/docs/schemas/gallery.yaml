components:
  schemas:
    GetArtworkResponse:
      success:
        type: object
        properties:
          message:
            type: string
            example: Get Artwork success
          data:
            $ref: '#/components/responses/ArtworkDataResponse'
      failed_401:
        type: object
        properties:
          message:
            type: string
            example: Get Artwork failed
          error:
            type: string
            example: no token provide
      failed_500:
        type: object
        properties:
          message:
            type: string
            example: Get Artwork failed
          error:
            type: string
            example: Error message
    
    AddArtworkRequest:
      type: object
      required: []
      properties:
        title:
          type: string
          example: title
        image:
            type: string
            format: binary
    AddArtworkResponse:
      success:
        type: object
        properties:
          message:
            type: string
            example: Add Artwork success
          data:
            $ref: '#/components/responses/ArtworkDataResponse'
      failed_500:
        type: object
        properties:
          message:
            type: string
            example: Get Artwork failed
          error:
            type: string
            example: Error message

    EditArtworkRequest:
      type: object
      required: []
      properties:
        title:
          type: string
          example: title
        image:
            type: string
            format: binary
    EditArtworkResponse:
      success:
        type: object
        properties:
          message:
            type: string
            example: Edit Artwork success
          data:
            $ref: '#/components/responses/ArtworkDataResponse'
      failed_500:
        type: object
        properties:
          message:
            type: string
            example: Edit Artwork failed
          error:
            type: string
            example: Error message

    RemoveArtworkResponse:
      success:
        type: object
        properties:
          message:
            type: string
            example: Remove Artwork success
          data:
            $ref: '#/components/responses/ArtworkDataResponse'
      failed_500:
        type: object
        properties:
          message:
            type: string
            example: Remove Artwork failed
          error:
            type: string
            example: Error message

      